default task="dev"

task dev {
    cmd #"""
      go run ../../cmd/gust \
        -e 'uv run server.py' \
        -p '?' \
        -h /health \
        -T \
        --optin comments \
        --exclude .venv
    """#
}
