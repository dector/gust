# Audio assets are stored in Git LFS. Builds must run `git lfs pull` first so
# `go:embed` sees real bytes instead of LFS pointer files.
*.ogg filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text
