task build {
    cmd "git lfs install --local && git lfs pull && go build -o out/gust ./cmd/gust"
}

task dev:self {
    description "Watch Gust source, rebuild, and restart the docs/demo app"

    cmd "tools/dev-self.sh"
}
